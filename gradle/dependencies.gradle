repositories {
    maven { url = 'https://files.minecraftforge.net/maven' }
    mavenLocal()
    mavenCentral()
    maven {
        url "https://plugins.gradle.org/m2/"
    }
}

dependencies {
    apiCompileOnly fg.deobf("com.ldtteam:domum_ornamentum:${project.minecraftVersion}-${project.domumOrnamentumVersion}:universal"){
        transitive = false
    }
    compileOnly fg.deobf("com.ldtteam:domum_ornamentum:${project.minecraftVersion}-${project.domumOrnamentumVersion}:universal"){
        transitive = false
    }
    runtimeOnly fg.deobf("com.ldtteam:domum_ornamentum:${project.minecraftVersion}-${project.domumOrnamentumVersion}:universal"){
        transitive = false
    }
}
